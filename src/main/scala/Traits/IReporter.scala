package Traits

trait IReporter {

/**
Lahav Harary 316012517
Omer Gez 313329302
*/

/**
 * A trait that will be used later on with three other classes named: "printReporter" and "wordReporter" and "textReporter"
 * The reason to use a trait is because those 3 functions are pretty similar (Each one of them "reports" but with a
 * different approach)
*/

  def report()

}
